{
  "name": "voluntra",
  "version": "1.0.0",
  "description": "VOLUNTRA: A webapge to find volunteers for events.",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "mon": "nodemon src/app.js",
    "build-react": "npx babel --watch react-src --out-dir public/react-build --presets @babel/preset-react"
  },
  "nodemonConfig": {
    "ignore": [
      "./public/**/*.js",
      "./public/**/*.html",
      "./public/**/*.css",
      "./react-src/**/*.jsx"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@babel/preset-react": "^7.0.0",
    "@babel/cli": "^7.0.0"
  }
}
